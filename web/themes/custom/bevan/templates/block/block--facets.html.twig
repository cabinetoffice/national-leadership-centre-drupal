<div{{ attributes }}>
  {{ title_prefix }}
  {% if label %}
    <h3{{ title_attributes.addClass('govuk-heading-s') }}>{{ label }}</h3>
  {% endif %}
  {{ title_suffix }}
  {% block content %}
    {{ content }}
  {% endblock %}
</div>
